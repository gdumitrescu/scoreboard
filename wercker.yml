box: oskarszoon/meteor-ci@1.2.1
services:
  - wercker/mongodb
packages:
  - meteor@0.6.4

build:
  steps:
    - script:
        name: setup
        code: npm install -g meteorite laika
    - script:
        name: test
        code: laika
env:
  NODE_VERSION: "0.10.11"
  METEOR_VERSION: "0.6.4"
  LAIKA_OPTIONS: "-t 5000"
